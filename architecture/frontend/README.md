# Frontend Component Library

This directory hosts reusable React components, styles, and build scripts to
speed up user interface development across projects.  It can be published as an
npm package or checked in as a workspace submodule.

Include components for:

- Chart containers with our purple theme
- File upload widget
- Navigation bar
- Authentication/login form
