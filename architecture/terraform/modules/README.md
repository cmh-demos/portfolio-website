# Terraform Modules

This directory will contain reusable GCP Terraform modules such as:

- `storage_bucket`
- `cloud_function`
- `iam_policy`

Each module exposes inputs and outputs consistent with our reference
architecture.
