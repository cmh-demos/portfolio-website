# CI/CD Templates

Reusable GitHub Actions workflows and helper scripts are stored here.  Projects
can reference these using `workflow_call` or copy them directly.

Common jobs:

- `lint-and-test.yml`
- `build-and-publish.yml`
- `terraform.yml`
